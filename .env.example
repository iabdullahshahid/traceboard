# Traceboard server connection (used by the SDK and demo script)
TRACEBOARD_PORT=4287
TRACEBOARD_HOST=http://localhost

# Demo script timing — increase for slower, screenshot-friendly playback
DEMO_DELAY_MS=400

# Set to true to loop the demo continuously
DEMO_LOOP=false
